# FDS-AMR risk register

Owner: Spec & Program Lead · Status: draft v0.4.21 (2026-09-26; changelog in README.md) · Source pin: FireX 36975d765f on branch `FDS-AMReX` (this repository; renamed from `AMReX`, D-037)

L = likelihood, S = severity (L/M/H). Status: open unless noted. Reviewed at every milestone. Code references were re-checked at FireX `36975d765f` in v0.2. AMReX references are to the local checkout `(local AMReX checkout)`.

| ID | Risk | Cause | Impact | L | S | Mitigation | Owner | Trigger / early warning | Status |
|---|---|---|---|---|---|---|---|---|---|
| R-01 | Default FFT pressure solver cannot be used for a composite multi-level solve | `PRESSURE_SOLVER_FFT` (pres.f90:318) calls Fishpak `H3CZSS`/`H3CSSS` (pois.f90) on one tensor-product block per mesh, with stretching in ≤2 directions (init.f90:2356-2366). Blocks couple only via iterated Dirichlet H (pres.f90:97-143, main.f90:1623-1743). FireX leaves this path unchanged. | Must replace it with MLMG/HYPRE. Slowdown: one MLMG solve ≈ 6–18× one FFT solve on a single box (Pressure Lead estimate, `pressure/02-performance-expectations.md`). Results differ from the FFT baseline. | H | H | Composite solve (FR-030). Uniform single-level mode uses `amrex::FFT::Poisson` (`AMReX_FFT_Poisson.H:31`; C++-only) behind the composite-solver interface; `pois.f90` is not ported (FR-037, D-021, ADR-001 v0.2). Estimated 30–45 ns/cell per solve vs FDS FFT 14–15 ns/cell × k iterations (break-even k ≈ 2–3); MLMG only once a refined level exists (FR-039). FFTW is installed and AMReX is configured with `AMReX_FFT=ON` (A-23 closed). Measure k and the pressure share of the step first (A-22). Compare against GLMAT baseline, which equals single-mesh FFT (FR-002, eps_H). | AMR Pressure Solver Lead | `FFT::Poisson` uniform-mode cost > 1.25× FDS FFT in the P2/Phase 2 measurement (typical k < 3); FDS inputs with mixed open/closed faces fall outside `FFT::Poisson` (A-22; stretched inputs no longer count, since they stay FDS-only in every mode, ADR-002 v0.2.1) | open |
| R-02 | Pervasive global mesh state through pointers | `MESH_TYPE` (mesh.f90:16-354) holds all fields. `POINT_TO_MESH` remaps several hundred module pointers (mesh.f90:505-916) and is called ~200 times across 18 files (Legacy Mapper count at `ce1f659`: 393 of 396 module pointers, 200 calls from 152 routines; `inventory/point_to_mesh_calls.csv`). At FireX `36975d765f`: 214 non-comment call sites in 19 files (new `vtkf.f90` adds 9) and 402 `=> M%` associations (ADR-001 count). Re-anchoring is A-13. Kernels use bare names (`U`, `RHO`, `ZZ`...) bound to "current mesh". | Hidden coupling; each AMReX box needs to look like a mesh; subtle aliasing bugs; large edit surface. | H | H | Legacy Mapper read/write matrix (A-02) before code changes. ADR-001 chooses a box-as-mesh adapter vs refactor. Phase 2 equivalence gate catches regressions. | FDS Legacy Mapper | Inventory finds fields written outside their owning routine; FR-001 failures traced to stale pointers | open |
| R-03 | Fortran/C++ interoperability gaps | AMReX core is C++. FDS is Fortran 2018 with derived types, allocatable components and pointers, which don't map directly to C. Checked gaps in AMReX `Src/F_Interfaces` (Integration Lead, 2026-09-25). **Pressure:** only cell-centred `amrex_poisson`/`amrex_abeclaplacian`. Variable density works from Fortran (`set_acoeffs`/`set_bcoeffs`, `AMReX_abeclaplacian_mod.F90:15-16`; HYPRE bottom solver, `AMReX_multigrid_mod.F90:9-14`). Nodal projection, `MacProjector`, EB and overset/obstruction-masked operators have no bindings, and tensor coefficients are unconfirmed. **Particles:** fixed pos/vel/id/cpu struct (R-28). **Unverified:** GPU support and how actively AMReX maintains the Fortran interface (A-15). | Driver redesign, per-step copies (IR-005 violation), schedule slip. If ADR-003 picks EB, the pressure path must be C++; particles may force C++ regardless. | L | M | **Re-scoped 2026-09-25 (D-027):** the Fortran-interface route (Option B) is rejected and the C++ driver is decided, so the `F_Interfaces` gaps no longer threaten the architecture. The residual risk is the `bind(C)` kernel boundary: `ISO_C_BINDING` only, no derived types across it. Earlier mitigations: `driver-options.md` verdict table. Phase 1 spikes (ADR-001 S1/S2). `ISO_C_BINDING` only; no derived types across the boundary. Settle ADR-003 before ADR-001 is accepted, because the pressure-path verdict depends on it. Keep the pressure path outside the `POINT_TO_MESH` shim (ADR-001 leaning). `amrex::FFT::Poisson` (FR-037) is C++ only (C++20 templates, no `F_Interfaces` bindings): a Fortran-driver Option B needs an estimated 100-200 line `extern "C"` wrapper (input to ADR-001, not a C++ mandate). | AMReX Integration Lead | A kernel needs derived-type or pointer data that cannot cross `bind(C)` | open (re-scoped by D-027) |
| R-04 | Coarse-fine conservation in the low-Mach formulation | FDS computes scalar fluxes independently on each side of a mesh interface from area-averaged ghost values (wall.f90:315-347). No refluxing was found: `EXTERNAL_WALL_TYPE%FVN/FVNS` (type.f90:478-479) are unreferenced. The divergence D depends on thermodynamic sources, so any mass defect feeds into the pressure equation. | Mass/species/energy drift; pressure/divergence inconsistency at interfaces. | H | H | Flux registers (FR-024) and composite budget diagnostics (FR-020..022) from Phase 3. Legacy Mapper confirms the current behaviour (A-03). | AMR Chief Architect | Cumulative imbalance > 1e-10 relative on species_conservation cases | open |
| R-05 | Subcycling without synchronization corrections hurts conservation or accuracy at coarse-fine interfaces (**Phase 6 risk only**) | Pressure Lead, 2026-09-25: with a global dt and a composite solve, the FDS step is a MAC projection with potential H (β = dt predictor, dt/2 corrector; velo.f90:1603-1630, 1723-1751), so no sync projection is needed through Phase 5. The risk arises only if subcycling is introduced (D-007): divergence reflux and dP0/dt background-pressure correction become necessary. | Divergence errors or background-pressure drift near interfaces in subcycled runs; FR-014/022/023 regress when subcycling is switched on. | L (M if D-007 schedules subcycling) | H | Gate moved from M4 to the **Phase 6 subcycling decision** (D-007, set by ADR-002 S1). Design sync corrections before subcycling is enabled. Phases 3-5 keep the plain checks: flux registers (FR-024), composite budgets (FR-020..022), interface divergence residual (FR-023). | AMR Pressure Solver Lead | S1 ratio > 1.5× (subcycling scheduled); FR-014/022/023 regress with subcycling on | open (retargeted 2026-09-25) |
| R-06 | OBST and wall-cell structures on refined levels and across regrids | OBSTs snap to each mesh's grid (`OBSTRUCTION_TYPE` I1..K2, type.f90:1082-1147; `BLOCK_CELL`, func.f90:5503). Wall cells (`WALL_TYPE`, type.f90:434) index `BOUNDARY_ONE_D`/`PROP1` state (solid temperature profiles, pyrolysis mass) and are built at setup (`INIT_WALL_CELL`, init.f90:2975). Matrix pressure solvers rebuild their matrices when obstructions change (`GLOBAL_MATRIX_REASSIGN`, main.f90:1806-1835). | Masks inconsistent between levels; solid thermal history lost or duplicated on regrid; large rework. | H | H | ADR-003 before Phase 5. FR-040 mask checker. FR-041 wall-state transfer tests. Option: never refine or coarsen across burning OBST faces early on. | AMR Chief Architect | Mask mismatches; solid mass jumps at regrid | open |
| R-07 | Complex geometry (CC_IBM/`&GEOM`) scope | ccib.f90 (24,046 lines) + geom.f90 (27,735 lines) and their own exchange paths (`MESH_CC_EXCHANGE`, called from main.f90:3135). | If required, a large extra phase. | M | H | Out of scope until ADR-003 and Q4 (FR-044 rejects it). | Spec & Program Lead | Project owner answers Q4 "required" | open |
| R-08 | Smokeview output assumes a static mesh list | `WRITE_SMOKEVIEW_FILE` (dump.f90:1766) writes one `GRID`/`PDIM`/`TRNX` block per mesh (dump.f90:2499-2511). Slice/boundary files are per mesh. | Refined-level output not viewable, or file counts explode with many boxes. | H | M | Q5 answered (owner): AMR runs write both Smokeview format, including refined levels, and VTK (FR-072, FR-074), so the Smokeview side is now tracked as R-43 (approach by ADR, A-40). The VTK route is unverified for AMR and needs HDF5, which CMake does not wire up (A-14). Pin Smokeview (and ParaView) versions. | AMR V&V Lead | Smokeview fails to load a Phase 3 case with >1 level | open |
| R-09 | Verification baseline drift | The baseline code, its Verification inputs and the expected results move whenever the pin moves. The pin already moved once (`ce1f659` → FireX `36975d765f`, D-005). | Comparisons become ambiguous; stored baselines silently stale. | M | M | Pin FireX `36975d765f` (D-005) through the spec phase. From the start of development FireX is merged into `FDS-AMReX` daily (D-034); baselines are re-pinned at each milestone, not at every merge, and each merge gets a quick Tier-1 smoke run (adopted plan pending V&V Lead confirmation, A-42). Store baseline outputs tagged with the commit. Rebuild and re-run every baseline on each re-pin. Daily-merge drift between re-pins is R-44; code-base drift is R-27. | AMR V&V Lead | Each milestone re-pin; a daily merge fails the Tier-1 smoke run; need for an upstream fix/feature appears | open |
| R-10 | Scope creep | Many FDS subsystems (HVAC, level set, HT3D, zones, tunnel preconditioner, CC_IBM, cylindrical) could each be asked to work under AMR. | Schedule overrun; unfinished core. | H | M | Supported-feature list per milestone; FR-004 rejection; change requests via the README decision log only. Q4 answered (D-033): thin OBSTs, HVAC, pressure ZONEs and level-set wildfire are in scope with refinement, which enlarges the core; GEOM/CC_IBM and HT3D are deferred; `TUNNEL_PRECONDITIONER` and cylindrical meshes are still open. | Spec & Program Lead | Requests outside the charter scope; unplanned work > 20% in a phase | open |
| R-11 | Bitwise equivalence infeasible | Changing storage, loop/tile order, OpenMP scheduling or MPI reduction order changes floating-point results. | Gate FR-001 fails for non-physical reasons; time wasted chasing ULPs. | H | M | Bit-parity limited to explicit kernels on frozen input against single-mesh FDS; whole runs judged at T2, since Crayfishpak is replaced (D-021, D-022). T1 as default for kernel checks; T0 only if Q1 demands it; single-thread comparisons. Box/rank independence per FR-005 with exact zone sums (R-36). | AMR V&V Lead | Differences at 1e-15 level from the first step with no logic change | open |
| R-12 | Machine limits | 8 cores, 16 GB, no GPU. | No scaling evidence beyond 8 ranks; large validation cases infeasible; GPU impossible. | H | M | Small anchor set (requirements §2.3); NFR-033 limited to 8 ranks; GPU required by D-027 but not testable here (R-38); ask for compute (Q6; partly answered: an owner-provided NVIDIA test machine is coming online, single GPU; multi-GPU and multi-node hardware, budget and date open, R-45). Machine time for the NFR-032 timing: R-37. | Spec & Program Lead | A needed case exceeds 12 GB or ~hours per run | open |
| R-13 | Dependency acquisition from public sources only | FireX CMake fetches HYPRE (and Sundials) by git (FetchContent, CMakeLists.txt:154-210); AMReX is also on GitHub. | Build blocked if public fetch is rate-limited or offline. | M | M | Test fetch early (A-06); mirror under `(local reference-code clones)`; NFR-022 offline build. FireX HYPRE `63331f19c72b` is cached offline at `(local GNU toolchain setup directory)/cache/` (`FETCHCONTENT_SOURCE_DIR_HYPRE`, `FETCHCONTENT_FULLY_DISCONNECTED=ON`; verified offline 2026-09-25). | AMReX Integration Lead | FetchContent failures | open |
| R-14 | Global time step set by the finest level | Single global dt (main.f90:715, 891). `CHANGE_TIME_STEP_LOOP` repeats the whole predictor when dt is cut (main.f90:774-897). With refinement, fine cells force a small dt everywhere. | AMR cost benefit (NFR-032) not reached before subcycling. | H | M | Accept in Phases 3-5; measure; Phase 6 subcycling; choose the benefit case so it is realistic under global dt. | AMR Pressure Solver Lead | NFR-032 projection > 50% of uniform-fine cost at M4 | open |
| R-15 | Pressure-iteration semantics change | Users rely on `VELOCITY_TOLERANCE`/`MAX_PRESSURE_ITERATIONS` (defaults: read.f90:10161, cons.f90:559), which apply at solid and interpolated boundaries. Under the composite solve the interface iteration disappears. The solid iteration stays only if immersed-boundary forcing is kept (pressure E-1), and the baroclinic iteration stays in every case (FR-036, velo.f90:3216-3315; Pressure Lead, 2026-09-25). | Results differ from baseline multi-mesh FFT runs; user confusion. | M | M | Compare against GLMAT baseline; document the changed meaning; warn when interface-only tolerance settings are ignored. | AMR Pressure Solver Lead | FR-002 passes vs GLMAT but not vs FFT | open |
| R-16 | Radiation on overlapping levels | FVM angle sweeps per mesh; incoming intensity at interpolated boundaries is averaged over neighbour cells (radi.f90:4345-4353). Updates are spread over time steps and angles. | Wrong or double-counted radiative flux in covered regions; high cost if solved on all levels. | M | M | Radiation-level ADR (FR-060). Updated v0.4.17 (AMR Radiation Lead, `radiation/01-radiation-amr-spec.md` §2, §7): a level-0 (or fixed-level) solve with QR interpolation (L-1) under-resolves T⁴ emission in refined flames, so it is the first increment and a user option only; per-level sweeps coarse to fine (L-2) are the target default. Radiation energy sums over uncovered cells only (FR-060); box coupling per FR-062. | AMR Chief Architect (with AMR Radiation Lead) | Energy closure failure only in radiation cases | open |
| R-17 | Particle redistribution and coupling | Particles live per mesh (`LAGRANGIAN_PARTICLE` in `MESH_TYPE`) and migrate through send buffers (part.f90:4854; `MESH_EXCHANGE(7/11)`). Coupling arrays are per mesh. | Particle loss or duplication at regrid; coupling not conservative. | M | M | Particle ledger (FR-050); decide the container in a Phase 7 ADR. Attribute storage in the Fortran interface is R-28. | AMReX Integration Lead | Ledger imbalance ≠ 0 | open |
| R-18 | Restart format tied to static meshes | Per-mesh files `CHID_<NM>.restart` (dump.f90:559-561) written/read by `DUMP_RESTART`/`READ_RESTART` (dump.f90:3871, 4058). | No restart of AMR runs until a new checkpoint exists; old restarts unusable in AMR mode. | H | M | Keep FDS restart in uniform mode; new AMR checkpoint (FR-080); rank-count change is optional (FR-081). | AMReX Integration Lead | — | open |
| R-19 | Devices bound to mesh at setup | DEVC locations resolved to one mesh during input (read.f90 ≈14085-14118). | Device values jump or break after regrid. | M | M | Composite-grid sampling (FR-070); regression test with regrid across a device. | AMR V&V Lead | Device time series discontinuous at regrid | open |
| R-20 | Pressure zones and background pressure | `PBAR`, `D_PBAR_DT` per zone; zone volumes and integrals exchanged via `EXCHANGE_DIVERGENCE_INFO` (main.f90:845, 1065); zone setup in main.f90:2606. | Volume integrals double-count covered cells; dP0/dt inconsistency (links to R-05). | M | M | Composite-volume integrals (FR-034); multi-zone with leakage is Phase 4 (FR-034 ruling, final: owned-face mask, one leak node per zone pair, one ΔP per zone). | AMR Pressure Solver Lead | `zone_*` cases fail T2 with refinement | open |
| R-21 | Threading model conflict | FDS uses loop-level OpenMP inside mesh routines (67 `!$OMP PARALLEL` regions in divg, velo, mass, radi, pres, turb, wall, func). AMReX uses `MFIter` tiling with its own OpenMP. | Oversubscription or races; slower than baseline. | M | M | NFR-012 design choice in ADR-001; run the `Thread_Check` race tests. | AMReX Integration Lead | Hybrid runs slower than pure MPI at 8 cores | open |
| R-22 | Uniform-mode performance regression | Extra indirection, ghost fill through AMReX, and MLMG instead of FFT (≈ 6–18× per solve on a single box, Pressure Lead estimate). | NFR-030 missed; users see no reason to adopt. | H | M | Measure f_pres and the full step at M2a/M2 (A-22). `amrex::FFT::Poisson` as the uniform level-0 solver behind the composite-solver interface (FR-037, D-021; estimated 30–45 ns/cell per solve, 16–24 B/cell vs about 64 for MLMG; code-reading estimates, `pressure/02-performance-expectations.md` §7). Likely no longer at risk on one node if k ≥ 3 is typical; stays open until P2 measures. Profile early. Explicit-kernel cost (A-37, `amrex/p1-findings.md` §13.5): the unoptimised D-031 gather clip adds 25-40 % to the P1 loop time (about 80-110 ns/cell/stage, roughly 2.6× FDS's scatter clip, because each neighbour's clip is recomputed 7 times); mitigation is the two-phase form (per-cell clip terms computed once, then gathered; D-031 (f)), which is also cheaper on the GPU and must pass the same 47 checks plus a covered-cell check. | AMReX Integration Lead | > 1.25× baseline at M2 | open |
| R-23 | Input features unsupported in AMR mode | Stretched grids (`TRNX/Y/Z`) and cylindrical meshes don't fit uniform AMReX levels. Mesh alignment rules (init.f90:3184-3226) differ from AMReX nesting. | Some existing inputs cannot use AMR. | H | L | Clear rejection (FR-004, IR-002); document; Q4. Stretched (TRN) inputs: decided FDS-only in every mode of the port, including single-level uniform runs; AMR mode uses uniform grids on each level (owner decision; D-030; IR-002; ADR-002 v0.2.1). Cylindrical meshes: DEFERRED in AMR mode, rejected with a named error when combined with refinement (FR-004), kept in uniform mode (11 cases), revisited on a real user need (owner decision 2026-09-26, D-042). | Spec & Program Lead | — | open |
| R-24 | Existing "refinement" behaviour taken as a reference when it isn't one | Embedded meshes are one-way coupled (User Guide line 1112). Three `Adaptive_Mesh_Refinement` cases are commented out of `FDS_Cases.sh` (lines 4-6). `samr_notes.tex` in the Technical Reference Guide is an unreferenced draft with empty sections. | Wrong baseline expectations for refined cases. | M | M | Use analytic/uniform-fine references (FR-014), not embedded-mesh baselines; A-09 checks those cases. Related (D-028): FDS's shaped-OBST `AREA_ADJUST` depends on the mesh split and process count (confirmed by runs), so multi-mesh baselines with a CYLINDER split across meshes are not layout-independent references. FM_Burner (V&V; `vv/fm_burner_area_check.md`): in the 12 OBST-cylinder inputs the burner top is split over 4 meshes; with the burner meshes on one process the area adjustment gives 0.5208 (25/48) of the specified fuel flow (CH4: about 4.2 kW instead of 8.1 kW), while one mesh per process or a single mesh gives exactly 1.0. Upstream runs one mesh per process, so upstream results are fine, but the development machine can run the committed inputs only on 1 process (FDS ERROR(115)), so they are not FDS baselines here as committed; use a single-mesh or MPI_PROCESS copy. Named as a negative input for FR-005 (v) (A-08 sub-item answered). | AMR V&V Lead | Refined-case targets derived from embedded-mesh runs | open |
| R-25 | Team coordination across parallel leads | Six leads writing interdependent docs at the same time on a shared machine. | Contradictory assumptions; rework. | M | M | README decision/action logs as the single source; ADR dependency order (ADR-003 → ADR-002); review at M1. | Spec & Program Lead | Two docs state conflicting decisions | open |
| R-26 | Per-box `POINT_TO_MESH` shim becomes a permanent crutch or costs too much | ADR-001 option: remap the module pointers onto AMReX box data (Fortran 2003 bounds remapping) so the 152 calling routines run mostly unchanged. Module pointers are process-global, so boxes run one at a time per rank. Per-mesh side data (`WALL`, `CELL_INDEX`, `EXTERNAL_WALL`, coordinates `X`, `RDX`) must be rebuilt per box at every regrid. Mesh-number-indexed state (`OMESH(NOM)`, `EXTERNAL_WALL%NOM`, `NMESHES`-sized arrays) breaks when box ids change. | No OpenMP tiling over boxes and no GPU path while the shim is in place; regrid cost dominates; kernel extraction keeps getting postponed. | M | M | Treat the shim as a CPU-only transition step with an exit milestone in the roadmap. Under D-027 the exit rule sets only when kernels move to the device; the port itself is required (NFR-043, roadmap Phase 11). Measure the regrid rebuild cost in the Phase 2 prototype. `inventory/module_globals.csv` flags mesh-number-indexed state and lower-bound or `IBAR`-extent assumptions before ADR-001 is accepted. P1 shim requirements from inventory checkpoint 2 are R-35. | AMR Chief Architect | Regrid rebuild > 10% of step time on the prototype (proposed); no kernel-extraction date set by M4 | open |
| R-27 | FireX upstream drift and divergence from `firemodels/fds` master | The branch `FDS-AMReX` (renamed from `AMReX`) is cut from FireX `36975d765f` (D-005); it is also on the project owner's fork as `marcosvanella/fds:FDS-AMReX`, commits are authored under a dedicated project identity, and pushes need the project owner's approval (D-037). FireX keeps moving, and master is diverging from it: about 8.6k source lines in 22 files already differ, mostly `pres.f90`, `main.f90`, `dump.f90`, `read.f90`, `radi.f90`. Those same files are the refactor's main edit surface. | Each rebase re-opens citations, baselines and merged AMR edits. Upstream fixes (in either line) are hard to take. A future upstreaming target (Q3) may no longer match. | H | M | Rebase policy decided (D-034, owner decision; Q10 answered, A-17 closed): the branch stays read-only on `36975d765f` until the spec phase ends; once development starts, FireX is merged into `FDS-AMReX` daily, so each merge is small, but baselines and citations drift between milestone re-pins (R-44, A-42). Following `firemodels/fds` master is part of the upstream-later decision (Q3, decided for now: the project owner's fork). At each milestone, record the FireX-vs-pin and master-vs-pin diffstat for the core files (main, pres, velo, mass, divg, wall, mesh, type). Isolate AMR code behind `USE_AMREX` and new modules to keep the rebase surface small. Re-run baselines on every rebase (R-09). | Spec & Program Lead | FireX or master diff in `pres.f90`/`main.f90`/`mesh.f90` > ~1k lines since the pin (proposed), or a needed upstream fix appears | open |
| R-28 | FDS particle attributes do not fit the AMReX Fortran particle interface | The Fortran `amrex_particle` is a fixed `bind(C)` struct with `pos`, `vel`, `id`, `cpu` and no extra real/integer components (`AMReX_particlecontainer_mod.F90:18-23`). The container exposes only add, get, count, redistribute and write (`:28-32`). FDS `LAGRANGIAN_PARTICLE_TYPE` (type.f90:389-429) has about 36 component declarations, including mesh-local indices into surface, 1-D conduction, radiation and wall storage (`BC_/OD_/B1_/B2_/BR_/WALL_/CFACE_INDEX`) (Integration Lead, 2026-09-25). | A side array must be re-synchronised on every redistribute and regrid, keyed by particle ID. That brings extra MPI traffic, ordering bugs and particle-state loss (FR-050..052 failures). The alternative is the C++ `ParticleContainer` route, which pushes ADR-001 toward C++ independently of the pressure path. | H | M | Treat particles as a separate ADR-001 driver. Before ADR-001 is accepted, run the ADR-001 S2 counter-spike: side-array prototype vs C++ container with runtime components on a small case. Attribute-integrity test by particle ID (FR-050). Keep FDS particle storage per box until Phase 7 if needed. | AMReX Integration Lead | S2 shows the side array needs its own MPI exchange or per-step copies (IR-005); any attribute mismatch after redistribute | open |
| R-29 | GPU speed of `PoissonHybrid` unmeasured | Corrected 2026-09-25 (Pressure Solver Lead, AMReX `99ddfda`): `amrex::FFT::PoissonHybrid` does run on the GPU. `AMReX_FFT_Poisson.H:715-780` has a device branch, a `ParallelFor` over the x-y columns with a serial Thomas solve down each z column. Only performance is open: TODO at :716-718 (cuSPARSE, or make z unit-stride). `PoissonHybrid` matters only for meshes stretched in z. | z-stretched uniform runs could be slow on the GPU; no correctness impact. | M | L | Measure `PoissonHybrid` GPU performance once GPU hardware exists (A-29, R-38). If slow, options are the TODO's cuSPARSE or z unit-stride layout, or MLMG. Owner decision: stretched cases stay FDS-only (D-030), and ADR-002 v0.2.1 applies this to every mode, so `PoissonHybrid` is not used. The FireX HYPRE GPU path risk formerly in this row is now R-40. | AMR Pressure Solver Lead | GPU profile shows the z-column solve dominating the step on a z-stretched case | **closed: moot** (ADR-002 v0.2.1: stretched cases stay FDS-only in every mode, including single-level uniform runs, so `PoissonHybrid` is not used; A-29 closed) |
| R-30 | One HYPRE build must serve both FDS and AMReX, and versions do not match | FDS/FireX links HYPRE for GLMAT/UGLMAT/ULMAT, while AMReX MLMG can use HYPRE as bottom solver. Two copies in one executable are not viable. AMReX CI tests HYPRE 3.1.0. The development machine has a genuine 3.0.0 (`(local GNU third-party library tree)/libs/hypre/v3.0.0`, static, CPU-only, OpenMPI-matched; `HYPRE_config.h`: `v3.0.0-0-gda9f93f8d`). FireX CMake fetches commit `63331f19c7` (CMakeLists.txt:160-164), which is `v2.32.0-24` (unreleased), although CMake labels it "3.0.0" and the FDS run header prints that label; FireX rejects the development machine's 3.0.0 (`find_package(HYPRE 2.32.0)`, CMakeLists.txt:196). Build provenance is the source of truth (requirements §2.1). Reference results depend on FireX's HYPRE settings (PMIS coarsening, l1-Jacobi relaxation; imkl.f90:244, 251) (Pressure Lead / Integration Lead, 2026-09-25). | Link failures or API mismatches, or baseline HYPRE results shift if HYPRE is changed for AMReX. Today the two already differ: the FireX baseline links 2.32.0-24, the AMReX install links 3.0.0. This would invalidate FR-016/FR-038 references. | M | M | Decided (D-026): production AMReX builds link FireX's `63331f19c7` (v2.32.0-24, ≥ AMReX's 2.20.0 minimum), keeping FR-016/FR-038 references like-for-like and matching the HYPRE configuration (MPI, OpenMP, integer size). P1 and P2 stay on the interim 3.0.0 AMReX install and record the exact HYPRE build. A-27: build a second AMReX install against a GNU build of the pinned HYPRE (offline cache `(local GNU toolchain setup directory)/cache/hypre-63331f19c72b`) and re-run the smoke tests and P2's MLMG+HYPRE check. Keep reference runs on the FireX build (requirements §2.1). Pin in NFR-021. | AMReX Integration Lead | AMReX configure/link errors against the chosen HYPRE; a HYPRE change moves baseline `H` beyond eps_H | **closed 2026-09-25** (D-026 done): AMReX `99ddfda` at `(local AMReX install built against HYPRE 2.32)` links the pinned HYPRE v2.32.0-24 (`(local GNU third-party library tree)/libs/hypre/63331f19/lib/libHYPRE.a`, static) and P2's MLMG+HYPRE solve uses it: every P2 log reports `HYPRE (v2.32.0-24-g63331f19c - HEAD branch)`, and the HYPRE bottom matches the default bottom to 1.3e-15 (`amrex/p2-findings.md` §5; A-27). FireX reference runs link the same pin. History: configure-and-link half done on the interim 3.0.0 install; step 1 (pinned install). |
| R-31 | GPU rank model mismatch between FireX and AMReX | FireX lets several ranks share one GPU through resource sets (`FDS_RANKS_PER_GPU`, main.f90:5096; gather to set masters, pres.f90:3411-3421). AMReX MLMG expects about one rank per GPU (Integration Lead, 2026-09-25; not verified on hardware). | The required GPU phase (D-027, roadmap Phase 11) cannot keep both models; rank layout, load balance and FR-038 behaviour conflict. | M | M | Record as a GPU-phase design constraint (D-027 makes the GPU phase required; target per Q11). Do not bake a rank-per-mesh assumption into the AMR driver. Link to R-40 for the CPU-side protection of the FireX path. NFR-047 (owner requirement) now requires at least 1 rank per GPU with several ranks per GPU allowed; at AMReX `99ddfda` all ranks use device 0 when one GPU is visible (AMReX_GpuDevice.cpp:292-293), so several ranks per GPU is a configuration and cost question (NVIDIA MPS, MLMG coarse-level efficiency), not a hard block; A-43 specifies it. A-43 delivered (`amrex/multi-gpu-mpi-spec.md`): AMReX divides the default device arena among the ranks sharing a GPU (AMReX_Arena.cpp:427), and several ranks per GPU need MPS for concurrent kernels; FireX's resource-set variable is read from the job environment (main.f90:5108). Owner follow-up: one CPU core per GPU is too slow for FDS, so the design must support several cores per GPU, either several ranks per GPU through NVIDIA MPS (configuration (a), close to FireX's resource-set model) or one rank per GPU with OpenMP threads and/or asynchronous I/O (configuration (b)); A-43 compares them and A-44 measures the default on the test machine. Host-side bottlenecks: R-46. | AMReX Integration Lead | Q2 answered "GPU is a goal"; GPU hardware becomes available | open |
| R-32 | Fragile machine environment | A machine reset on Sep 23 wiped apt packages (MPI, compilers) while `(local workspace)` survived. The GNU Build Chief reinstalled OpenMPI 5.0.7 and GNU 14.2 on 2026-09-25 and scripted it (A-21 closed). The Intel Build Chief restored oneAPI at the pre-reset versions under `/opt/intel/oneapi` on 2026-09-25 (A-18 closed). FFTW headers were installed on 2026-09-25 (A-23 closed). | A repeat reset would block anything needing both toolchains (NFR-020, A-08's Intel build, M2). Silent toolchain drift makes baselines non-reproducible. Stale-pin risk (Debian mirrors keep only current versions) is now mitigated by the local .deb cache (A-21). Remaining minor gap: recommended-only packages are not cached (e.g. `libhwloc-plugins`, harmless with `HWLOC_LIBXML=0`). | H | M | Scripted toolchain reinstall and check (A-21): Intel half done (`(local Intel third-party library tree)/install_oneapi.sh`, idempotent, pinned, self-verifying; source `(local Intel third-party library tree)/env_oneapi.sh` before Intel builds or `libimf.so` is not found; notes in `(local Intel third-party library tree)/README_oneapi.md`); GNU half done (`(local GNU toolchain setup directory)/install_gnu_ompi.sh`, idempotent, 31 pinned Debian 13 packages, `--check` mode, exit 2 on a missing pin; env `(local GNU toolchain setup directory)/env_gnu_ompi.sh`; notes `(local GNU toolchain setup directory)/README.md`; offline FireX HYPRE source cache `(local GNU toolchain setup directory)/cache/hypre-63331f19c72b`). Local .deb cache done (GNU Build Chief): `(local GNU toolchain setup directory)/cache/debs/`, 206 .debs (31 pins + 175 dependencies at validated versions), 234 MB, `MANIFEST.tsv` and `SHA256SUMS`; the installer tries the checksum-verified cache first (never removes or downgrades), then the mirror at pinned versions, then `ALLOW_UNPINNED=1`; flags `--source`, `--refresh-cache`, `--dry-run`; `--check` 20/20; offline simulation restored 74 packages, 0 removed. Toolchain check in every milestone's entry criteria (roadmap). Record compiler/MPI/HYPRE versions with every baseline (A-08). | Spec & Program Lead (with GNU and Intel Build Chiefs) | `which ifx mpifort gfortran` fails at milestone entry; compiler version differs from the recorded baseline | open |
| R-33 | Stateful-wall coverage freeze limits AMR use cases | Phase 5 proposal D-010/FR-041a: wall faces with solid-phase history keep their level and the coarse-fine boundary never cuts them. | Broader than burning faces: any `SURF` with a `MATL_ID` carries 1-D conduction history from step 0, so nearly every non-inert wall is stateful before the first regrid, and the freeze means static refinement near all such walls (AMR Chief Architect, 2026-09-25; in ADR-003 v0.2). Flame spread over burning surfaces (the most attractive AMR use case) cannot be followed by refinement until FR-041b. The fine region can inflate around large objects with conducting walls (memory, NFR-031). | M | M | Choose a stateless-burner NFR-032 case (D-008). Accepted for the Phase 5 burner case (D-010). FR-041b target stays TBD until ADR-003 cross-level transfer Spike G2 gives a cost estimate. Report fine-region growth caused by the freeze in Phase 5 tests. No flame-spread claims before FR-041b. | AMR Chief Architect | Fine-cell count > 2× the tagged region because of the freeze; users request flame-spread refinement | open |
| R-34 | Baseline FDS may not pass on the FR-016 case itself | `ns2d_16_int_1to2_refinement` (with `ns2d_16_emb_1to1/1to2`) was committed disabled in `042aa2624d` (R. McDermott, 2024-11-08) as a demonstration of "errors in both embedded mesh strategy and interpolated refinement" (`FDS_Cases.sh` lines 4-6). There are no dataplot rows, so pass/fail is undefined, and it is unknown whether the derived UGLMAT-HYPRE copy runs on this 13-mesh periodic 2:1 case (vv/test-plan.md §9.2, D-5). | FR-016 has no valid reference; the Phase 3 unit checks (a)-(c) and the Phase 4 T2 check (d) are gated on a baseline that may be broken or meaningless. | M | H | Run `int_1to2` and its derived UGLMAT copy on the FireX build first (A-09), on both toolchains plus the Debug build, before treating FR-016 as a gate. V&V defines pass as "runs to T_END with no NaN" and records the analytic-error values as baseline values. FR-014 is judged against analytic and uniform-fine references, not these baselines (R-24). If the UGLMAT copy fails, FR-016 goes back to the Spec Lead and Pressure Lead for a replacement case. | AMR V&V Lead | A-09 run shows NaN, a setup stop or no convergence on `int_1to2` or its UGLMAT copy | open |
| R-35 | P1 shim needs more than a pointer remap, and hidden global state blocks per-box kernels | Inventory checkpoint 2 (Legacy Mapper, FireX `36975d765f`): `RHO`/`ZZ` are read over `-1:IBAR+2` under the default SUPERBEE limiter (mass.f90:70-76; cons.f90:710), so each box needs 2 indexed ghost layers; `ZZ` is written into the ghost ring (mass.f90:477-479); each box face needs external-wall entries reached through `CELL%WALL_INDEX` (mass.f90:831-836); FDS lower bounds (0/-1) must be preserved. `MESHES(1)%` is used about 100 times as a global holder for slice/VTK output state, and `POINT_TO_MESH(1)` is hard-coded at vtkf.f90:1875 and 2482. `module_globals.csv` flags 326 globals as blocking pure kernels. | Per-box shim is larger and slower than planned (R-26); output breaks when box 1 is not mesh 1 or changes at regrid; kernel extraction (and any GPU path) stays blocked by global state. | H | M | P1 builds the shim with 2 ghost layers, FDS lower bounds and per-box external-wall entries (`inventory/kernel_footprint_mass.md`). Move `MESHES(1)` output/VTK state into a dedicated output module before regrid work; decide its fate with Q5 (VTK output, FR-072/074). Use `module_globals.csv` to schedule global-state removal before kernel extraction (R-26 exit). Device kernels cannot use module pointers remapped through `POINT_TO_MESH` (their device addresses must come from the wrapper, not from host globals); they take box arrays as explicit-shape dummy arguments declared `has_device_addr` (fallback `is_device_ptr` on `type(c_ptr)`; ADR-001 v0.3), with separate array and loop bounds (IR-007); this is the same rewrite the C++ `ParallelFor` variant needs (D-027). Kernels also write into neighbours' local copies at runtime (exchange code 18, obstruction mass, main.f90:3960-3964; `MESH_CC_EXCHANGE`; `EXCHANGE_GEOMETRY_INFO`), so the per-box interface must provide neighbour geometry and cell data, not only ghost fields (IR-005). Radiation (AMR Radiation Lead, `radiation/01-radiation-amr-spec.md` §5; merged v0.4.17): the update counters `RAD_CALL_COUNTER`/`ANGLE_INC_COUNTER` are per mesh (mesh.f90:339) and the flags `UPDATE_ALL_ANGLES`/`EXCHANGE_RADIATION` are host globals (cons.f90:186, 188; set at radi.f90:3855-3857 and main.f90:1795), so they must become per-level or global driver state before radiation kernels can be pure device kernels (with R-46). | AMR Chief Architect (with FDS Legacy Mapper) | P1 fails the face-index round-trip or ghost-width checks; any output routine reading `MESHES(1)` after regrid | open |
| R-36 | Reproducibility requirements FR-005 not met because global reductions are order-dependent | AMReX has no reproducible reduction (`ParallelDescriptor::ReduceRealSum` is a plain `MPI_Allreduce`), and its CPU reductions (`MultiFab::Dot`, MLMG and gauge norms) use OpenMP `reduction(+:)` with unspecified combine order. FDS zone integrals `DSUM`/`PSUM`/`USUM` (main.f90:2038-2040) feed `D_PBAR_DT`, which mass.f90 reads, so an order-dependent sum breaks explicit-stage bit-parity in zone cases. All global reductions are now catalogued (A-25 closed, `inventory/global_reductions.csv`): 259 rows, 91 order-dependent floating-point sums feeding 13 quantities (DSUM/PSUM/USUM, `RAD_Q_SUM`/`KFST4_SUM` at main.f90:1760-1761, HVAC nodes, device statistics feeding controls, synthetic-eddy means, setup-time areas and volumes); 68 of the 91 are per-process loops over meshes before the MPI call. DT and iteration tests use MIN/MAX (order-independent). | Results change with box split, rank count or thread count; FR-005 (i)-(ii) fail; run-to-run differences hide real regressions. | M | M | Exact fixed-point (e.g. int128) summation for zone integrals and the pressure gauge (FR-005 (ii); negligible cost). Fixed thread count and `OMP_DYNAMIC=false` for reproducibility runs (FR-005 (iv)). P2 measures actual `H` differences across 1/2/4 ranks and repeats runs 3 times per path. Exact accumulation starts at the per-cell/per-box sum, not only at the Allreduce (FR-005 (ii)); setup-time areas and volumes are summed over the whole domain exactly (D-028, FR-005 (v)). Inventory done (A-25 closed). | AMReX Integration Lead | P2 repeat runs differ at a fixed layout; zone cases differ across rank counts | open |
| R-37 | Not enough machine time for the NFR-032 benefit case | One shared 8-core development machine, and timing needs the idle machine (D-12). The uniform-fine reference (64,64,160, A-19) is estimated at ≈ 2.3 h per run (0.5–4.5 h), so a median of 3 needs ≈ 7 h of exclusive machine time. AMReX and FDS builds and other jobs compete for the same cores. | NFR-032 timing slips or is measured on a busy machine, making the ≤ 50% ratio unreliable. | M | M | Exclusive window booked (A-26: Fri to Sat, go/no-go, fallback Sat); P1 and P2 stay out of it. Level-0 timing runs use `max_grid_size = 16 16 8` (40 boxes, 5 per rank; D-024) so rank imbalance does not inflate the cost ratio. Measure one run before planning repeats. Fall back to McCaffrey (D-008) if the reference is too slow. Linked to R-12. | AMR V&V Lead (with Spec & Program Lead) | First reference run exceeds 5 h, or no 7 h idle window is found before M1 timing | open |
| R-38 | GPU requirement cannot be verified on the development machine | The development machine has no GPU, so NFR-043 (full time step on the device, D-027) cannot be verified here; only compile-only GPU-backend builds and host builds of the OpenMP-offload variant are possible. | Device correctness, performance and the no-transfer rule stay unproven until GPU hardware is available; porting errors surface late. | H | H | GPU test machine (owner decision, 2026-09-25, relayed by the Chief Architect; charter Q11 (b)): an owner-provided NVIDIA test machine (wording corrected), coming online; no date yet (A-31). It has a single GPU; multi-GPU and multi-node verification is R-45. AMD is out of scope (D-027). Add a routine CUDA device-compile build (D-029; host-only does not count) once the NVIDIA HPC SDK is installed (A-31). Run host fallback for logic checks, and keep the kernel inventory (device-capable yes/no) current. | Spec & Program Lead | No GPU access by the start of Phase 11; GPU-backend build cannot be installed on the development machine | open (an owner-provided NVIDIA test machine coming online; no date) |
| R-39 | nvfortran OpenMP-offload maturity and interop with AMReX device memory | Re-scoped 2026-09-25 (v0.4.4) after the owner decision (ADR-001 v0.3): K2 is Fortran with simple OpenMP `target` offload (`!$omp target teams loop` plus `map`/`collapse`), compiled with nvfortran; AMReX field data is never mapped. Open question: does nvfortran accept `has_device_addr` on explicit-shape Fortran dummy arrays? Fallback: `is_device_ptr` on `type(c_ptr)` arguments with `c_f_pointer` inside the target region (IR-007; A-31 settles it). Also: stream synchronization (AMReX kernels run asynchronously on AMReX's stream while target regions run on the compiler's queue, so every boundary needs synchronization or wiring onto AMReX's stream) and sharing one CUDA runtime. OpenMP `target` is not an AMReX GPU backend, and the AMReX GNU-make docs list OpenMP offload only for IBM compilers (GPU.rst:140); device pointers do pass to OpenMP via `is_device_ptr` (GPU.rst:1457-1530). OpenMP-offload performance relative to C++ `ParallelFor`/CUDA is unmeasured; CUDA Fortran is allowed for profiled hot loops (with OpenMP fallbacks) for that reason. AMD is out of scope (D-027, owner decision), so AMD Fortran offload and HIP interop are no longer part of this risk. | K2 may need the `c_ptr` fallback, pay a sync at every kernel boundary, or run slower than K1; CUDA Fortran hot loops are NVIDIA-only. | M | M | First K2 device compile with the NVIDIA HPC SDK (A-31, D-029) records whether `has_device_addr` works or the fallback applies. Test stream wiring and device-data passing on a GPU when available (R-38). Document synchronization rules at kernel boundaries. Compare K2 vs K1 speed in P1 once a GPU is available; a second-compiler compile (ifx) is optional and non-gating (NFR-045, A-36). `amrex/driver-options.md` §5. | AMReX Integration Lead | nvfortran rejects both `has_device_addr` and the `c_ptr` fallback, or K2 cannot share AMReX's device memory, stream or CUDA runtime | open |
| R-40 | FireX HYPRE GPU pressure path broken without anyone noticing | FireX offloads the HYPRE solve to GPUs (`HYPRE_DEVICE_RUN`, cons.f90:569; e.g. pres.f90:1178-1184) and gathers matrices to resource-set masters (`FDS_RANKS_PER_GPU`, main.f90:5096; pres.f90:3411-3421). The development machine has no GPU (R-38), so the AMR refactor of `pres.f90`/`main.f90` cannot exercise that path. | The GPU capability that motivated D-005 regresses unnoticed. It is discovered only when GPU hardware is available. | M | M | FR-038: CPU runs of HYPRE cases with resource sets on and off. Compile-check the `WITH_HYPRE_DEVICE` code paths if a GPU-enabled HYPRE build can be made without a GPU (TBD). Keep AMR changes out of the GPU-specific branches of `pres.f90`. Moved from R-29 on 2026-09-25 (v0.4.1); D-027 answers Q2 (GPU required). Future rank-model conflict: R-31. | AMR Pressure Solver Lead | Any AMR edit touching `HYPRE_DEVICE_RUN` or `MPI_COMM_RS` code; FR-038 failures | open (moved from R-29) |
| R-41 | Compiler miscompile in the Intel reference build | The Intel FireX `-O3` build failed G0 (superseded by the `-O2` rebuild, G0 PASS): `pois.f90` compiled by ifx 2026.1.1 at `-O3` with OpenMP gives wrong results even with one thread; `-O2` or no OpenMP fixes it; most likely an ifx optimizer bug, not proven, no minimal reproducer yet (Intel Build Chief; `(local build directory)/firex-36975d7/impi_intel_rel_O3_miscompile/provenance/G0_FAILURE_DIAGNOSIS.md`, kept with the superseded `-O3` build). Some affected runs still end with normal completion. | Before the fix the Intel baseline was invalid and Intel confirmations (A-08, FR-016 Intel half, NFR-020) waited; a silent miscompile could also hit other files or later compiler versions. | L | M | **Mitigated:** the V&V Lead approved a whole-build `-O2` deviation (all Fortran and HYPRE C; OpenMP stays on), recorded in `impi_intel_rel/provenance/PROVENANCE.md`; the rebuilt Intel Release passes G0 (A-08). Rating lowered from H/H: the Intel baseline is valid again, and the residual is a silent miscompile elsewhere or in a later ifx. Report the bug upstream to Intel once confirmed with a minimal reproducer; keep open until then. Keep G0 checking results, not just completion. Intel `-O2` timings are not directly comparable to an `-O3` build (GNU reference is `-O3`); NFR-030 does not name a compiler, so any timing comparison (FDS baseline vs AMReX) must use the same compiler and optimisation level on both sides. | Intel Build Chief (with AMR V&V Lead) | Any Intel G0 case differs from GNU FireX beyond T2, or a new ifx version is adopted | open (until the upstream report) |
| R-42 | Legacy domains used as level 0 coarsen shallowly in MLMG | With agglomeration (default) the MLMG depth is set by how many times each level-0 cell count divides by 2 (`AMReX_MLLinOp.H:1276-1315`); semicoarsening leaves at most one direction uncoarsened (:1318-1321). Legacy FDS domains often have cell counts with few factors of 2 (e.g. `race_test_1` 34×18×32 stops at 17×9×16). The level-0 blocking factor must also divide the domain, so such domains need small level-0 blocking factors (A-35), but the shallow coarsening comes from the domain size, not from the blocking factor. | Large direct or slow bottom solves on legacy-sized domains; NFR-030/NFR-032 timing misses blamed on the solver. | M | M | FR-010 input-time warning with the coarsest grid size, recorded in timing runs (NFR-030 rule (6)). Remedies in order: remesh level 0 to cell counts with more factors of 2 (free at run time, CPU and GPU); then HYPRE as bottom solver, which on GPU builds (D-027, NFR-043) depends on a separate CUDA build of HYPRE (dependency only; our HYPRE builds are CPU-only) or pays GPU-host copies per solve. Changing `blocking_factor` is not a remedy. | AMR Pressure Solver Lead | Coarsening warning on a timing or anchor case; bottom solve dominates the pressure time | open. P2 confirmed 34×18×32 → 17×9×16 (4896-cell bottom solve) for max_grid_size 32/16/8, with HYPRE bottom agreeing to 1.3e-15 (`amrex/p2-findings.md` §7). |
| R-43 | Smokeview output from a changing AMR hierarchy | Owner decision on Q5 (2026-09-25): AMR runs write Smokeview-format output, including refined levels, and VTK; Smokeview cannot read VTK (FR-072, FR-074). FDS declares its Smokeview mesh set once at setup: `main.f90:216-221` calls `WRITE_SMOKEVIEW_FILE` (dump.f90:1766), which writes `OFFSET`, `GRID`, `PDIM`, `TRNX`/`TRNY`/`TRNZ` and `OBST` per mesh in one `DO NM=1,NMESHES` loop (dump.f90:2457, 2467, 2499, 2505, 2511-2529, 2541). Afterwards the `.smv` file is only appended to (rank 0 reopens it with `POSITION='APPEND'`, main.f90:255; per-step entries flushed by `WRITE_STRINGS`, main.f90:651), and no mesh block is added during a run. Output files bind to a fixed mesh index and fixed index bounds: `SLCC`/`SLCF` entries name their mesh (dump.f90:1260, 1264), each slice file header carries fixed `I1..K2` written once at setup (dump.f90:1215), boundary files are declared per mesh (dump.f90:1471, 1473), and run-time `PL3D` entries name the mesh (dump.f90:6842) against a per-mesh grid file written at setup (dump.f90:1056). Options: (a) fixed output meshes declared at setup and filled from the composite solution at output time (averaged or injected): fits the existing `dump.f90` unchanged and the Smokeview mesh set never changes (Chief Architect's starting preference); (b) refined boxes written as static Smokeview meshes per output interval or per regrid, possibly as separate `.smv` segments: needs a new mesh list at every regrid, which nothing in FDS does, and Smokeview support for a mesh set that changes during a run is unverified (ASSUMPTION: not checkable from FDS source; the Smokeview source is not in the tree); (c) level-0-only Smokeview output plus full-resolution VTK: **rejected**, fails the owner decision. Output writing runs on the host under D-027, so no option touches the GPU time step (Chief Architect). | Under (a), refined data appears in Smokeview only at the output meshes' resolution unless output meshes are made fine wherever refinement can occur, which inflates file size and host memory at output time. Under (b), cases may fail to load in Smokeview, and the file count grows with boxes × regrids. Either way the FR-072 Phase 9 exit slips. | M | M | **Spec Lead first estimate, to be refined by the Chief Architect and Integration Lead (A-40).** Relative size: (a) **M**: mesh declaration, file binding and the append-only `.smv` logic are reused; the new work is choosing the output meshes and a host-side composite-to-output-mesh fill (average or injection) at each output time, plus the open resolution question (FR-072 open point (1)). (b) **L**: a new `.smv` writer path per regrid, file management across regrids, and an unverified Smokeview-side dependency. Likelihood M because (a) has a clear fit to existing code; impact M because a poor choice is found only at Phase 9. Mitigation: A-40 tests (a) and (b) on a small case after the first prototypes and records the choice in an ADR; the Legacy Mapper lists every output routine that loops over meshes; keep the FR-072 D test in G13 (Smokeview 6.11.2 is not installed on the development machine, `vv/environment.md` §8); changing the Smokeview source is a charter non-goal (§4), so option (b) cannot be rescued on the Smokeview side; the VTK route proceeds in parallel (FR-074, needs HDF5, A-14). Related: R-08. Output-step host cost bears on the choice between this risk's options (option (a) adds a host-side composite-to-output fill at each output time); the A-44 measurement of output steps (NFR-047 cores per GPU, R-46) is an input to A-40. | AMR Chief Architect (with AMReX Integration Lead) | The small-case test shows (a) cannot represent refined-level data adequately and (b) does not load in Smokeview; or Smokeview is still not installable by Phase 9 entry | open |
| R-44 | Daily-merge drift breaks bitwise references and line citations | D-034 (owner decision, 2026-09-25): once development starts, FireX is merged into `FDS-AMReX` daily, replacing the frozen `36975d765f` pin. Any merge can change FDS numerics or move code in the files the refactor edits most (`pres.f90`, `main.f90`, `dump.f90`, `read.f90`; R-27). | Bitwise references (D-022; FR-005 comparisons against single-mesh FDS; NFR-046 waiver sites by file:line) go stale or fail for reasons unrelated to AMR work, or pass against an outdated baseline. Line citations in these documents and in teammates' files silently point at the wrong code. | H | M | Adopted plan (the project coordinator's suggestion, pending V&V Lead confirmation, A-42): re-pin V&V baselines and source line citations at each milestone; every daily merge gets a quick Tier-1 smoke run; every spec citation names the milestone pin it refers to (requirements §2.1). Proposed additions for A-42 to accept or drop: a bitwise failure between re-pins is first re-checked against a baseline rebuilt at the merged commit before it counts as an AMR defect; record the merged FireX commit in each run's provenance (R-09); keep AMR code behind `USE_AMREX` and new modules to keep merge conflicts small (R-27). | AMR V&V Lead (citations: Spec & Program Lead) | A daily merge fails the Tier-1 smoke run; a merge touches a file with cited lines between re-pins; A-42 rejects or changes the plan | open |
| R-45 | Multi-GPU and multi-node verification has no hardware | NFR-047 (owner requirement) requires MPI runs on multiple NVIDIA GPUs across nodes, at 1 and at more than 1 rank per GPU. The development machine is one node without a GPU (R-12, R-38). An owner-provided NVIDIA test machine (coming online; model not recorded; no date yet) has a single GPU. No cluster is named, and budget and date for multi-GPU hardware are open (charter Q6). | Only single-GPU runs (1 and several ranks on one GPU) and CPU-only runs up to 8 ranks on one node can be verified. Rank-to-GPU mapping across GPUs, GPU-aware MPI, inter-node halo exchange and FR-005 behaviour on several GPUs stay unproven, so the NFR-047 acceptance cannot close. | H | M | Verify what one GPU allows on the test machine: 1 against several ranks on one GPU (with and without NVIDIA MPS as A-43 specifies) against FR-005. Verify the CPU-only MPI+OpenMP path on the development machine (NFR-010, NFR-012). A-43 delivered the recommended configuration and test list (`amrex/multi-gpu-mpi-spec.md`): tests L-1 to L-12 run on the test machine's single GPU (§7.1); tests M-1 to M-9 (rank-to-GPU mapping across GPUs and nodes, inter-node GPU-aware MPI, rank-count invariance across nodes, scaling, NUMA affinity, AMR load balance across GPUs, the chosen cores-per-GPU route on a multi-GPU node, MPS at scale) need multi-GPU or multi-node hardware (§7.2), so multi-GPU runs can start as soon as hardware exists. The global-rank modulo mapping can be unbalanced on uneven layouts (AMReX_GpuDevice.cpp:300-301), which only multi-GPU tests can show (NFR-047). Ask the project owner for multi-GPU and multi-node hardware, budget and date (Q6, A-11). | Spec & Program Lead (with AMReX Integration Lead) | Phase 11 entry with no multi-GPU or multi-node hardware identified | open |
| R-46 | Host-side work throttles GPU runs | Under D-027 the time step runs on the device, but input, setup, any kernel not yet ported and, mainly, output stay on the host: Smokeview and VTK files are written there (FR-072, FR-074, R-43). Owner follow-up (2026-09-25): one CPU core per GPU is too slow for FDS. Neither way of adding host cores is free. Configuration (a), several ranks per GPU through NVIDIA MPS, means more, smaller boxes per GPU and more halo traffic; MPS is not covered by the AMReX checkout. Configuration (b), one rank per GPU with threads or asynchronous I/O, meets writers that are not threaded: `dump.f90` and `vtkf.f90` have no OpenMP directives, and `dump.f90` uses module-state mesh pointers (`POINT_TO_MESH`, 10 calls). AMReX `AsyncOut` covers AMReX's own plotfile, MultiFab and particle writers, not the FDS Fortran writers, and needs `MPI_THREAD_MULTIPLE` when there are fewer files than ranks (NFR-047; AMReX_AsyncOut.cpp:35-44; IO.rst:97-132). | Output steps and unported host stages dominate wall time while the GPU idles; NFR-047 is met functionally but GPU runs lose their speed advantage. The chosen default may need writer changes (snapshot copies, per-thread-safe writers) that were not planned. | M | M | A-43 compared (a) and (b) (`amrex/multi-gpu-mpi-spec.md` §5): AMReX `async_out` covers only AMReX-native output, not the FDS Smokeview/VTK writers, which need host snapshots and a refactor or dedicated I/O ranks; in GPU builds AMReX's own host loops are single-threaded, so OpenMP helps only host code the project threads. MPS support on consumer and mobile GPUs is not documented by NVIDIA either way (spec §2), so configuration (a) on the test machine depends on test L-3; test-machine GPU memory and core count are unknown (spec §8 items 2 and 12). A-44 measures time per step, including output steps, at 1 core per GPU, (a) and (b) on the test machine, and the default is recorded in NFR-047 and a decision. Keep host-side output writing on snapshot copies where cheap, so that it can move to a background thread later. The Smokeview approach choice (A-40, R-43) takes the output-step cost into account. Porting progress (NFR-044 kernel inventory) shrinks the unported host stages. | AMReX Integration Lead | A-44 shows that neither configuration keeps output steps acceptable (threshold set in the A-44 method by the V&V Lead), or the chosen writer route needs `dump.f90` changes beyond the plan | open |
| R-47 | Masked (non-box) level 0 has no plain FFT pressure solve | The 40 FR-006 cases whose meshes do not form a box (for example the §2.3 restart anchors `device_restart_a/b/base_case`, `simple_duct`, `hallways`) stay IN because the gaps between meshes become solid level-0 cells (Integration Lead confirmation; final ruling Chief Architect; IR-002). `FFT::Poisson` solves a whole rectangular domain with one BC type per face (FR-037), so a level 0 with solid regions has no plain whole-domain FFT, while D-021 and D-032 (5) make the FFT the solver whenever the hierarchy is a single uniform level. | These cases may need a different uniform-mode solver path (FFT over the bounding box with the gap cells treated like obstructions and the obstruction iteration, or MLMG), with different iteration counts, cost (NFR-030) and agreement with FDS, where each mesh's FFT treats the gap as an exterior wall; or they drop out of FR-006. | M | M | FR-037 open point: the Pressure Solver Lead answers with the Integration Lead (raised), before Phase 2 exit, since FR-006's uniform subset gates at Phase 2. V&V flags the vents that become fluid-solid boundaries in the A-46 rerun. | AMR Pressure Solver Lead (with AMReX Integration Lead) | The chosen route fails T2 against FDS on a non-box case, or no route is chosen by Phase 2 entry | open |
| R-48 | Smokeview output breaks when boxes change ranks | Q9 (D-036) lets AMReX load balancing override `MPI_PROCESS` in AMR mode, so box ownership changes during a run. FDS's writers assume each mesh, and its output files, stay with one rank for the whole run (the `.smv` mesh list is written once at setup, main.f90:216-221). | Missing, duplicated or inconsistent Smokeview files after a redistribution; output that differs with rank count; FR-072 and FR-076 fail. | M | M | FR-076 rank-independent box-to-file mapping plus a gather or ownership rule at write time; covered in the Integration Lead's MPI/GPU/I-O write-up (A-48) and decided together with the A-40 output approach (fixed output meshes make the file set independent of ownership). | AMReX Integration Lead (with AMR Chief Architect) | An FR-076 test shows different output after a redistribution or at a different rank count | open |
| R-49 | Stiff chemistry on the GPU unavailable or slow | Finite-rate chemistry integrates each cell with CVODE on the host (`CVODE_SERIAL`, chem.f90:753). A device path needs batched SUNDIALS/CVODE or a custom stiff integrator that fits the chosen kernel style (D-029, NFR-044); a batched integrator that shares step control across a box also conflicts with distribution independence (NFR-035). Proposed by the AMR Species & Combustion Lead (`combustion/01-species-combustion-amr-spec.md` §6). | "Full step on GPU" (D-027, NFR-043) blocked for finite-rate cases; host fallback throttles GPU runs (R-46). | M | H | Early spike on a batched device integrator after the P1 readability review (D-029); port the explicit integrators first; require per-cell error control or an explicit FR-005 exemption (NFR-035). L/S: Spec Lead rating. | AMR Species & Combustion Lead | Spike shows no device integrator with per-cell control, or device chemistry slower than host | open |
| R-50 | Clipping after interpolation or regrid breaks round-off conservation | Per-cell clipping of ρ or Y after prolongation, regrid fill or average-down changes mass without a matching flux (FR-020, FR-021, FR-024; D-023). D-031 covers only the in-level `CHECK_MASS_DENSITY` clip, not clips in interpolation or regrid. Proposed by the AMR Species & Combustion Lead (`combustion/01-species-combustion-amr-spec.md` §6). Not related to R-22. | Composite mass or species budgets miss round-off tolerances on refined runs; silent drift in species totals. | M | M | Volume-weighted `average_down` of ρY; limited conservative interpolation (convex averaging); count and report every clip that fires. Related: R-04, D-031. L/S: Spec Lead rating. | AMR Species & Combustion Lead | Nonzero clip counter or FR-020/FR-021 imbalance on regrid steps only | open |
| R-51 | Exact cross-box radiation sweep does not scale | Option S-A of FR-062 (charter Q12) orders the sweep across boxes per angle (pipelined across ranks) to stay independent of the box split (FR-005 (i)). The dependency chain serialises work across boxes and ranks. Proposed by the AMR Radiation Lead (`radiation/01-radiation-amr-spec.md` §7). | Radiation dominates runtime on GPUs or many ranks; the S-B fallback costs FR-005 (i) byte-identity across box splits. | M | H | Decide Q12 by M2 with its FR-005 status recorded (FR-062); keep one per-box sweep kernel shared by S-A and S-B; measure S-A cost early on `radiation_gas_panel` split into boxes (A-50). L/S: Spec Lead rating. | AMR Radiation Lead | S-A sweep time per step grows with rank count on the development machine, or on the test machine's GPU | closed 2026-09-26 (D-039: S-B is the default and S-A is not required; reopen if the Radiation Lead proposes S-A as an option) |
| R-52 | Lagged box coupling loses accuracy as boxes shrink | Option S-B of FR-062 exchanges box-face intensities once per angle cycle, so intensity lags one cycle per box crossed (FDS behaviour per mesh, radi.f90:4345-4353; main.f90:1110-1116; FDS_User_Guide.tex:5894). AMReX boxes can be much smaller than FDS meshes (floor 16 cells). | Radiative flux less accurate than FDS multi-mesh for the same domain; results depend on the box split. | M | M | S-B is the decided default (D-039; FR-005 (i) exemption for box-split dependence recorded in FR-005). Allow FDS-style radiation iterations to tighten the coupling; consider a minimum box size for radiation; set the tolerance class before M2 by comparing against FDS multi-mesh on the split `radiation_gas_panel` (A-50). L/S: Spec Lead rating. | AMR Radiation Lead | Radiative flux differences growing with box count on A-50 | open |
| R-53 | Angle-parallel radiation exceeds GPU memory | Angle-parallel device sweeps hold cells × angles in flight × bands: about 170 MB per 1M cells at 21 angles, and 832 MB per band for all 104 angles (Radiation Lead estimate, `radiation/01-radiation-amr-spec.md` §5; not measured). The radiation spec cites NFR-031, but that is the 12 GB host limit on the development machine; the device limit is the NFR-047 arena on a GPU whose memory is not yet recorded. | Out-of-memory on the device or forced small angle batches that slow the sweep. | M | M | Process band by band; size angle batches from the NFR-047 arena; measure on the test machine's GPU (A-44). L/S: Spec Lead rating. | AMR Radiation Lead | Arena exhaustion in radiation kernels; angle batch size forced below the per-step angle count | open |
| R-54 | Regrid loses radiation history | FDS spreads radiation updates over time steps and angles and keeps time-shared state: gas `UIID` (restart at dump.f90:3921) and wall `ILW` (radi.f90:4745-4773; `PACK_BOUNDARY_RADIA`, func.f90:5128-5146). New fine patches start without it. Proposed by the AMR Radiation Lead (`radiation/01-radiation-amr-spec.md` §7). | Flux transients at new fine patches after each regrid; wall heating errors near moving patches. | M | M | FR-063: carry `ILW` as wall state (FR-041) and prolong `UIID`, or run a full-angle update after each regrid (open, spec §7 Q4); test with a regrid inside the run (A-50). L/S: Spec Lead rating. | AMR Radiation Lead | QR or wall-flux jumps at regrid times in A-50 | open |
| R-55 | Covered coarse walls double-counted | Wall records exist on every level where an OBST face lies; HRR, solid mass loss, `OB%MASS` and wall devices sum all of them unless covered coarse records are excluded. FDS already skips covered cells in the zone `USUM` loop (divg.f90:757-767, test at :760), so the zone part is handled; HVAC `NODE_*` sums were not masked (FR-034 ruling). Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §3, SR-1). | Wrong HRR and fuel mass budgets on refined runs; FR-022 wall budget fails. | H | M | FR-045 (one active wall record, owned faces only); the FR-034 owned-face mask for leak and HVAC faces; exact sums per FR-005 (ii) and D-028; wall budget test (FR-022 walls, A-52). | AMR Solid Phase Lead | HRR or solid mass on a 2:1 variant exceeds the single-level value by about the covered area fraction | open |
| R-56 | 1-D solid solves diverge on the GPU | The solid kernel is sequential in depth, with variable node counts, adaptive per-cell sub-steps (wall.f90:2323-2325), renoding and layer removal (:2361-2804), per-material reactions and branching by backing and pyrolysis model; components are `ALLOCATABLE` (type.f90:230-262). Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §5, SR-2). | Warp divergence and a long tail of slow wall cells; wall work dominates GPU steps on refined surfaces. | M | H | NFR-048 (flat padded SoA, deterministic gather); wall cost in the load-balance weight (NFR-035); profile a pyrolysis case on the test machine's GPU when available. | AMR Solid Phase Lead | Wall kernel time per step ≫ gas kernels on a GPU profile | open |
| R-57 | Back-side lookups add a per-step exchange | `EXPOSED` backing reads the back face directly across meshes (wall.f90:1876-1883); in AMR mode front and back faces can be on different levels or ranks. Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §2, SR-3). | Extra communication every step; layout-dependent results if the lookup is not keyed independently of boxes. | M | M | FR-046 (face key plus exchange); batch the exchange with existing wall exchanges (`PACK_WALL`, func.f90:4482-4530). | AMR Solid Phase Lead (with AMReX Integration Lead) | Back-wall exchange visible in step timers; `back_wall_test` differs across layouts | open |
| R-58 | Burn-away and OBST removal with solid state on refined levels | Burn-away (wall.f90:2689-2697) and `CREATE_OR_REMOVE_OBSTRUCTIONS` (main.f90:1782) change the geometry during the run; with refinement the removed OBST can carry solid state on several levels, and masks, wall cells and pressure matrices must be rebuilt consistently (FR-042; R-06). Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §2, SR-4). | Mass or energy lost at burn-away; mask mismatches after removal; `box_burn_away*` cases fail. | M | H | FR-042 tests with refinement; R3-T mismatch list computed for every OBST at setup (FR-040); remove the state on every level together; A-52 variants. | AMR Solid Phase Lead (with AMR Chief Architect) | Solid mass jump at burn-away on a refined run | open |
| R-59 | Finest-ever wall records cost too much | The proposed FR-041b rule (option A) keeps 1-D records at the finest resolution that ever covered a face, so solid work and memory stay at the finest count after coarsening. Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §2, SR-5). Related: R-33. | Memory (NFR-031) and wall cost grow on large refined surfaces even after the fine level moves away. | M | M | ADR-003 Spike G2 measures option A before the FR-041b decision (open point, AMR Chief Architect); option B remap stays the fallback. | AMR Chief Architect (with AMR Solid Phase Lead) | G2 shows wall memory or time above the gas-phase cost on a flame-spread case | open |
| R-60 | Surface accumulators missed by transfer rules | Besides the 1-D profiles, wall records carry accumulated quantities such as `PART_MASS`, `A_LP_MPUA` (type.f90:302) and pyrolysis integrals; a transfer rule written for profiles can drop them. Proposed by the AMR Solid Phase Lead (`solid/01-solid-phase-amr-spec.md` §2, SR-6). Related: R-06. | Silent loss of deposited or accumulated mass at regrid; wrong ignition or burnout timing. | M | M | List every stateful `BOUNDARY_ONE_D`/`PROP1`/`PROP2` component in the FR-041 transfer rule (Legacy Mapper inventory: `mesh_fields.csv`); bitwise check of all wall components across a regrid (FR-041a acceptance). | AMR Solid Phase Lead | Any wall component changes across a regrid that should not | open |
